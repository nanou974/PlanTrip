name: domain
description: >
  Cœur métier de PlanTrip : entités, valeurs et contrats.
  Ne dépend d'aucune technologie ni d'aucun autre package du projet,
  conformément au principe d'architecture orientée domaine du DevBook.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
