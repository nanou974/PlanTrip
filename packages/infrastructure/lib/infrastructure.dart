/// Point d'entrée public du package `infrastructure`.
///
/// Squelette volontairement vide à ce stade : les implémentations
/// Drift de `TripRepository` et OSM de `LocationSearchService` /
/// `ReverseGeocodingService` seront ajoutées une fois le domaine
/// (les contrats qu'elles implémentent) en place.
library infrastructure;
